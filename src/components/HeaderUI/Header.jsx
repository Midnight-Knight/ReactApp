import React from "react";
import "./Header.css";
import documents from "./../../Image/documents.png";

export const Header = () => {
    return (
        <header id="header">
            <img src={documents} />
            <div>

            </div>
        </header>
    );
};